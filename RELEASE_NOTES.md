# Release v0.1.2

Release: v0.1.2 — Example app fixes, native bridge, and verification captures

Summary
- Align release tag with package.json (0.1.2)
- Implemented Android native fullscreen bridge (interstitial, interstitial video, rewarded) with reflection wiring to admixer AAR APIs
- Fixed AdMixer.init reflection signature to ensure mediaKey initialization
- Reworked example app (App.tsx) to remap adUnit IDs for correct formats and to gate native view usage safely
- Added mediation adapter inclusion and iterative testing for Pangle / AdFit / AdManager (build-time toggles)
- Captured verification artifacts (Android emulator + iOS simulator) under `example/ExampleHostApp/capture/` and created `captures_bundle.zip` with test artifacts

Important notes
- Some adUnits returned by the server are configured as banner vs fullscreen; we remapped interstitial tests to `104703` after server-side checks.
- The vendor SDK may require specific mediation adapters (Pangle, AdFit, AdManager, AppLovin, Unity). We added these incrementally for testing; some adapters may require minSdk >= 23 or bring transitive deps that need resolution.
- Ad fill may still be blocked by server-side targeting or lack of adapter presence; if you need guaranteed test creatives, coordinate with the ad ops team to whitelist the emulator/device/media_key.

Artifacts
- All screenshots and filtered logs are committed at:
  - `example/ExampleHostApp/capture/`
  - `example/ExampleHostApp/captures_bundle.zip`

How to create a GitHub Release from this tag (v0.1.2)
1. Open: https://github.com/glenn-yu/NapSspReactNativePluginSample/releases
2. Click "Draft a new release"
3. Tag version: `v0.1.2` (already pushed)
4. Release title: `v0.1.2 — Example fixes & verification captures`
5. Paste content from this file into the release description
6. Attach `example/ExampleHostApp/captures_bundle.zip` if you want a downloadable artifact
7. Publish release

If you want, I can:
- Create a release draft automatically using a GitHub personal access token you provide (securely), or
- Just commit this RELEASE_NOTES.md (done) and you can publish via web UI (fast), or
- I can open the Releases page for you (if integrated into your UI surface).

--
Generated by the verification run on 2026-04-18
