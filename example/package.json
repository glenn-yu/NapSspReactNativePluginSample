{
  "name": "nap-ssp-example",
  "version": "0.0.1",
  "private": true,
  "description": "Workspace shim so `npm run start:example` from the repo root reaches ExampleHostApp.",
  "scripts": {
    "start": "npm --prefix ExampleHostApp run start",
    "android": "npm --prefix ExampleHostApp run android",
    "ios": "npm --prefix ExampleHostApp run ios",
    "test": "npm --prefix ExampleHostApp test"
  }
}
