module.exports = {
  dependency: {
    platforms: {
      android: {
        sourceDir: 'android',
      },
      // iOS podspec is automatically detected if this is empty or contains specific paths.
    },
  },
};
