# react-native-nap-ssp (sample)

This repository is a sample scaffold for the NapSsp React Native plugin described in the provided plan document.

What I added:
- Converted plan doc (docs/nap_ssp_plan.txt) and summary (docs/summary.md)
- Minimal TypeScript placeholder implementation under src/
- package.json and README
- Basic GitHub Actions workflow for CI (build/test placeholder)

Next steps (automated by agent if you want):
- Implement native Android (NapSspModule.kt, BannerViewManager) and iOS (NapSspModule.swift) bridge code
- Add example app in /example and connect native modules
- Add tests and linting, publish config

This is a working repo scaffold; core native integration still required.
